# Where the "Access prototype" buttons point.
# Unset, the landing page falls back to http://localhost:5180 — the P0 prototype's dev server.
# Copy this file to .env.local for local overrides, or set the variable in the deploy environment.
VITE_PROTOTYPE_URL=https://prototype.brezel.cc
